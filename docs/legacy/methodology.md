# Methodology

The system uses point-in-time features, rolling windows, and chronological train/test splits to reduce look-ahead bias. Dynamic graphs represent bank interdependence using correlations, tail co-movement, ownership overlap, balance-sheet profile similarity, and credit stress proxies. Stress scenarios inject exogenous shocks and propagate them through the normalized adjacency matrix.
